
<form class="postCreation" action="/post/search" method="post">
   
    <select name="category">
    <% categories.forEach((category) => { %>
  
    <option value="<%= category.id %>"><%= category.name %></option>
    <% }) %>
  </select>
  <button type="submit">Submit</button>
</form>